/* Abaixo é apresentado uma validação de erro de tipo ao descomentar o código, se espera um número e foi atribuída uma string */

/* function soma(a: number, b:number){
  return a + b
}

soma(1, 'a') */

/* No caso abaixo é uma propriedade escrita que não setada na criação da interface */

/* 
interface IAnimal {
  nome: string
  tipo: 'terrestre' | 'aquático'
}

const animal: IAnimal = {
  nome: 'Elefante',
  tipo: 'voador'
}
 */